# Backend API base URL (use when backend is on different host/port)
# For Vite dev, use proxy in vite.config.js and leave empty or set to ""
VITE_API_URL=

# If backend runs on different origin, set full URL e.g.:
# VITE_API_URL=http://localhost:5000/api
