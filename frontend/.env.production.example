# Build with same-origin API (Nginx proxies /api and /socket.io to Node).
# Copy to .env.production before: npm run build
#
# No raw EC2 IP in the bundle — paths only (matches your approved guide pattern).

VITE_API_URL=/api
VITE_WS_URL=
